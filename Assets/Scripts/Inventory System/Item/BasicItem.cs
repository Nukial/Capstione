using UnityEngine;

public class BasicItem : Item
{
    public BasicItem(ItemData data) : base(data)
    {
        // Bạn có thể thêm các thuộc tính và phương thức đặc biệt cho BasicItem ở đây nếu cần
    }
}
